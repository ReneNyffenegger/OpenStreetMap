Remove records with unnecessary keys in table tag:
  o  source
  o  image

Create a table building.
  Attributes:
    building-type
    street
    house_number
    ? zip_code
    ? city
  (see queries/addresses.sql)

Interessante tags:
  key=note

  key=network          val=ZVV
  key=public_transport val=stop_area
  key=railway          val=station
  key=operator         val=SBW

  key=ele

Uninteressante tags:
  source
  openGeoDB:%

